# No hace falta definir variables para levantar el proyecto.
# Los defaults estan hardcodeados en:
# - src/shared/config/runtimeConfig.ts
# - vite.config.ts
#
# Recomendado para VS Code:
# 1) Copia este archivo como .env.local para desarrollo local.
# 2) Si quieres simular un build de produccion local, crea .env.production.local.
# 3) Usa los launch configs de .vscode/launch.json.
#
# En desarrollo, si no defines VITE_API_BASE_URL, se usa por defecto:
# http://127.0.0.1:8000 (via proxy /API de Vite)

# ---- Desarrollo local (.env.local) ----
# Solo se necesita sobrescribir la API base cuando sea necesario:
# VITE_API_BASE_URL=http://127.0.0.1:8000
#
# Otras opciones (observabilidad, flags de debug) están hardcodeadas
# en `src/shared/config/runtimeConfig.ts` y no deben añadirse aquí.
