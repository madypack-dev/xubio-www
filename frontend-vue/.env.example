VITE_API_BASE_URL=
VITE_USE_MOCKS=true
VITE_FALLBACK_TO_MOCKS_ON_ERROR=true
VITE_VERBOSE_STARTUP_LOGS=false
VITE_DEBUG_REMITOS=false
# En dev, si VITE_API_BASE_URL es absoluto (ej: http://127.0.0.1:8000),
# Vite crea proxy para /API y evita CORS en navegador.
# Se recomienda 127.0.0.1 en lugar de localhost para evitar problemas de proxy por IPv6 (::1).
# En produccion, si frontend y backend comparten origen, dejar VITE_API_BASE_URL vacio
# para consumir /API relativo y evitar CORS por diferencias localhost/127.0.0.1.
# VITE_VERBOSE_STARTUP_LOGS=true muestra diagnosticos de arranque en consola.
# VITE_DEBUG_REMITOS=true muestra observaciones mapeadas en la vista de Remitos.
