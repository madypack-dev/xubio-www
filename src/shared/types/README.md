# Shared Types

Tipos y contratos compartidos entre modulos.

- `valueObjects.ts`: value objects minimos de dominio (`TransaccionId`, `ClienteId`, `ProductoId`, `FechaComercial`, `Money`) y funciones de normalizacion.
